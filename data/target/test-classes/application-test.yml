# Perfil TEST: excluye MongoDB para tests unitarios sin infraestructura real
# Se activa con @ActiveProfiles("test") en los tests de contexto completo
# SB4: paquetes autoconfigure movidos a org.springframework.boot.mongodb.* y boot.data.mongodb.*

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.mongodb.autoconfigure.MongoAutoConfiguration
      - org.springframework.boot.data.mongodb.autoconfigure.DataMongoAutoConfiguration
      - org.springframework.boot.data.mongodb.autoconfigure.DataMongoRepositoriesAutoConfiguration
      - org.springframework.boot.mongodb.autoconfigure.metrics.MongoMetricsAutoConfiguration

logging:
  level:
    com.rehabiapp: WARN
