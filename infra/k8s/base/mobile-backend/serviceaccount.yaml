# ServiceAccount dedicado para el Mobile Backend BFF
# Cada microservicio tiene su propio ServiceAccount (principio de minimo privilegio)
# La anotacion IRSA se anade en el overlay de AWS
apiVersion: v1
kind: ServiceAccount
metadata:
  name: mobile-backend-sa
  namespace: rehabiapp-mobile
automountServiceAccountToken: false
